import { CheckoutForm } from "@/components/checkout/checkout-form";

export const metadata = {
  title: "Checkout",
  description: "Complete your order via WhatsApp — Mobile Money accepted, nationwide delivery.",
};

export default function CheckoutPage() {
  return <CheckoutForm />;
}
